# xkeen_rotate

**Официальный продукт команды простовпн**

Автоматическая ротация прокси-серверов для Xray/Xkeen на роутерах OpenWRT/Entware.

Для клиентов сервиса [@prstabot](https://t.me/prstabot)  
💬 Служба поддержки: [@prsta_helpbot](https://t.me/prsta_helpbot)

## 🚀 Быстрая установка

Установка одной командой:

```bash
curl -sSL https://raw.githubusercontent.com/andrchq/xkeen_auto/main/install.sh | sh
```

или

```bash
wget -qO- https://raw.githubusercontent.com/andrchq/xkeen_auto/main/install.sh | sh
```

## ✨ Возможности

- ✅ Автоматическая проверка доступности серверов каждые 2-10 минут
- ✅ Автоматическое переключение при недоступности текущего сервера
- ✅ Загрузка и парсинг подписок (vless + reality)
- ✅ Telegram уведомления о важных событиях
- ✅ Резервное копирование конфигураций (последние 10 копий)
- ✅ Фильтрация технических серверов
- ✅ Автоматическая синхронизация подписки раз в день
- ✅ Команда `prosto` для быстрого управления
- ✅ Графический интерфейс через whiptail/dialog
- ✅ Оптимизированные конфигурации Xray (inbound/routing)
  - Блокировка рекламы и аналитики
  - Умная маршрутизация трафика
  - Оптимизация для популярных сервисов

## 📋 Требования

- OpenWRT/Entware роутер
- Установленный Xray/Xkeen
- curl или wget
- base64
- netcat
- cron

## 🎮 Команда prosto

После установки доступна команда `prosto` для быстрого управления:

```bash
prosto                   # Интерактивное меню
prosto status            # Показать статус серверов
prosto force             # Принудительная ротация
prosto test              # Тест Telegram уведомлений
prosto sync <URL>        # Синхронизация подписки
prosto cleanup           # Очистка технических серверов
prosto logs              # Просмотр логов (последние 30 строк)
prosto logsf             # Просмотр логов в реальном времени
prosto edit              # Редактировать настройки
```

## 🔧 Ручная установка

Если автоматическая установка не подходит:

```bash
# 1. Создать директории
mkdir -p /opt/root/scripts
mkdir -p /opt/etc/xray/outbounds_available
mkdir -p /opt/etc/xray/configs
mkdir -p /opt/etc/xray/backups

# 2. Скачать скрипты
cd /opt/root/scripts
wget https://raw.githubusercontent.com/andrchq/xkeen_auto/main/xkeen_rotate.sh
wget https://raw.githubusercontent.com/andrchq/xkeen_auto/main/xkeen_sync.sh

# 3. Установить права
chmod +x xkeen_rotate.sh xkeen_sync.sh

# 4. Установить команду prosto
mkdir -p /opt/bin
wget https://raw.githubusercontent.com/andrchq/xkeen_auto/main/prosto -O /opt/bin/prosto
chmod +x /opt/bin/prosto
export PATH="/opt/bin:$PATH"
echo 'export PATH="/opt/bin:$PATH"' >> /etc/profile

# 5. Настроить Telegram в xkeen_rotate.sh (строки 16-19)
vi xkeen_rotate.sh

# 6. Загрузить подписку
./xkeen_sync.sh "https://your-subscription-url"

# 7. Активировать первый сервер
./xkeen_rotate.sh

# 8. Настроить cron (ротация и автозапуск)
crontab -e
# Добавить:
*/5 * * * * /opt/root/scripts/xkeen_rotate.sh >/dev/null 2>&1
0 3 * * * /opt/root/scripts/xkeen_rotate.sh --sync-url="https://..." >/dev/null 2>&1
@reboot sleep 120 && /opt/root/scripts/xkeen_rotate.sh >/dev/null 2>&1
```

**Примечание:** `@reboot` запускает скрипт через 2 минуты после загрузки роутера.

## 🎛️ Рекомендуемые конфигурации Xray

Установщик предложит установить оптимизированные конфигурации `03_inbounds.json` и `05_routing.json`:

### Преимущества:

**Блокировка:**
- 🚫 Реклама (category-ads-all)
- 🚫 Аналитика (Google Analytics, Yandex Metrika)
- 🚫 QUIC для не-RU IP (UDP:443) - повышает стабильность

**Умная маршрутизация:**
- 🇷🇺 Русский трафик (.ru, .рф, Yandex, VK, Steam) → напрямую
- 🌍 Telegram, Discord, Google, ChatGPT, YouTube → через прокси
- 🔄 BitTorrent → напрямую (экономия трафика)
- 🏠 Локальный трафик → остается локальным

**Оптимизация:**
- Sniffing для определения протоколов HTTP/TLS
- Правильный порт 61219 для transparent proxy
- Поддержка TCP (redirect) и UDP (tproxy)

Существующие файлы автоматически бэкапятся перед заменой.

## 📱 Настройка Telegram уведомлений

Токен бота и ID группы уже встроены в скрипт (общие для всех клиентов).

**Вам нужно только получить индивидуальный ID топика:**
- Напишите администратору [@prsta_helpbot](https://t.me/prsta_helpbot)
- Получите ваш личный ID топика
- Укажите его в `xkeen_rotate.sh` (строка 18) или при установке

Каждому клиенту выделяется отдельный топик для уведомлений.

## 📊 Просмотр логов

```bash
# Через команду prosto
prosto logs              # Последние 30 записей
prosto logsf             # В реальном времени

# Напрямую
logread | grep xkeen_rotate | tail -20
logread -f | grep xkeen_rotate

# Логи cron
logread | grep cron | tail -10
```

## ⚙️ Настройки

Все настройки находятся в файле `xkeen_rotate.sh` (строки 3-22):

```bash
AVAILABLE_DIR="/opt/etc/xray/outbounds_available"  # где лежат все варианты
ACTIVE_DIR="/opt/etc/xray/configs"                 # где лежит активный outbound
BACKUP_DIR="/opt/etc/xray/backups"                 # резервные копии
BACKUP_KEEP=10                                     # сколько бэкапов хранить
TCP_TIMEOUT=10                                     # таймаут TCP-проверки (секунды)
RESTART_WAIT=10                                    # время ожидания после рестарта
CUSTOM_RESTART_CMD="/opt/bin/xkeen -restart"      # команда перезапуска
TG_BOT_TOKEN="..."                                 # токен бота
TG_CHAT_ID="..."                                   # ID группы
TG_TOPIC_ID="28"                                   # ID топика (0 для отключения)
TG_ENABLED=1                                       # 1=включено, 0=выключено
TEST_NOTIFY_ENABLED=0                              # 1=автотест каждые N минут
TEST_NOTIFY_INTERVAL=300                           # интервал в секундах
```

Для быстрого редактирования используйте: `prosto edit`

## 📱 Уведомления в Telegram

Система отправляет только **важные уведомления**:

### ⚠️ УЗЕЛ НЕДОСТУПЕН
Текущий сервер перестал отвечать, начат поиск альтернативы

### ✅ СМЕНА СЕРВЕРА
Успешно переключились на новый работающий сервер

### 🚨 ВСЕ СЕРВЕРЫ НЕДОСТУПНЫ
Критическая ситуация - не найдено ни одного работающего сервера

## 🗂️ Структура файлов

```
/opt/root/scripts/
├── xkeen_rotate.sh          # Основной скрипт ротации
└── xkeen_sync.sh            # Синхронизация подписки

/opt/bin/
└── prosto                   # Команда быстрого доступа

/opt/etc/xray/
├── configs/
│   ├── 03_inbounds.json     # Входящие подключения (redirect + tproxy)
│   ├── 04_outbounds.json    # Активная конфигурация outbound
│   ├── 04_outbounds.target  # Host:port активного сервера
│   └── 05_routing.json      # Правила маршрутизации
├── outbounds_available/     # Все доступные серверы
│   ├── 04_outbounds_US.json
│   ├── 04_outbounds_US.target
│   └── ...
└── backups/                 # Резервные копии (последние 10)
```

## 🔄 Автоматическая работа

После установки система работает полностью автоматически:

- 🔍 Каждые 2-10 минут проверяет доступность текущего сервера
- 🔄 При недоступности автоматически переключается на другой
- 📥 Каждую ночь в 3:00 обновляет список серверов из подписки
- 📱 Отправляет уведомления в Telegram о важных событиях
- 🔁 После перезагрузки роутера автоматически выбирает сервер (через cron @reboot)

## ❓ Решение проблем

### "curl: not found"
```bash
opkg update && opkg install curl
```

### "base64: not found"
```bash
opkg install coreutils-base64
```

### Уведомления не приходят
```bash
# Проверьте настройки
cat /opt/root/scripts/xkeen_rotate.sh | grep TG_

# Отправьте тест
prosto test
```

### Скрипт не запускается из cron
```bash
# Проверьте права
ls -la /opt/root/scripts/xkeen*.sh
# Должно быть: -rwxr-xr-x

# Если нет - установите
chmod +x /opt/root/scripts/xkeen*.sh
```

### Команда prosto не работает
```bash
# Проверьте установку
ls -la /opt/bin/prosto

# Если файл есть, но команда не найдена - добавьте в PATH
export PATH="/opt/bin:$PATH"
echo 'export PATH="/opt/bin:$PATH"' >> /etc/profile

# Или используйте полный путь
/opt/bin/prosto

# Переустановите если нужно
curl -sSL https://raw.githubusercontent.com/andrchq/xkeen_auto/main/install.sh | sh
```

### Автозапуск не работает после перезагрузки
```bash
# Проверьте наличие задания в cron
crontab -l | grep @reboot

# Если нет, добавьте вручную
crontab -e
# Добавьте строку:
@reboot sleep 120 && /opt/root/scripts/xkeen_rotate.sh >/dev/null 2>&1

# Убедитесь что cron запущен
/etc/init.d/cron status
/etc/init.d/cron start
/etc/init.d/cron enable
```

## 📝 Лицензия

MIT

## 💬 Служба поддержки

**По всем вопросам обращайтесь в [@prsta_helpbot](https://t.me/prsta_helpbot)**

- 💬 Техподдержка и настройка
- 📱 Получение ID топика для уведомлений
- 🔗 Получение ссылки на подписку
- 🤖 Подключение к сервису: [@prstabot](https://t.me/prstabot)

## 📧 О проекте

Официальный продукт команды **простовпн**  
Создан для обеспечения стабильности соединения клиентов

- 🛒 Покупка подписки: [t.me/prstabot](https://t.me/prstabot)
- 💬 Служба поддержки: [t.me/prsta_helpbot](https://t.me/prsta_helpbot)
- 📦 GitHub: [github.com/andrchq/xkeen_auto](https://github.com/andrchq/xkeen_auto)

