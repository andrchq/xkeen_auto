# Установка xkeen_rotate

**Официальный продукт команды [🤲🏻 простовпн](https://t.me/prstabot)**

Автоматическая ротация серверов для клиентов 🤲🏻 простовпн.

💬 Служба поддержки: [@prsta_helpbot](https://t.me/prsta_helpbot)

## Быстрая установка (рекомендуется)

Установка одной командой на роутере:

```bash
curl -sSL https://raw.githubusercontent.com/andrchq/xkeen_auto/main/install.sh | sh
```

или

```bash
wget -qO- https://raw.githubusercontent.com/andrchq/xkeen_auto/main/install.sh | sh
```

Установщик выполнит:
- ✅ Проверку и установку зависимостей
- ✅ Создание всех необходимых директорий
- ✅ Загрузку скриптов
- ✅ Интерактивную настройку Telegram
- ✅ Загрузку серверов из подписки
- ✅ Настройку автоматической ротации (cron)
- ✅ Настройку автозапуска после перезагрузки
- ✅ Самоудаление после установки

## Что вам понадобится

1. **ID топика** - получите у администратора [@prsta_helpbot](https://t.me/prsta_helpbot)
2. **URL подписки** - ваша личная ссылка на серверы

**Токен бота и ID группы уже встроены в скрипт** (общие для всех клиентов).

Напишите [@prsta_helpbot](https://t.me/prsta_helpbot) и администратор предоставит вам индивидуальный ID топика для уведомлений.

## После установки

Проверьте статус:
```bash
/opt/root/scripts/xkeen_rotate.sh --status
```

Просмотр логов:
```bash
logread | grep xkeen_rotate | tail -20
```

Отправить тестовое уведомление:
```bash
/opt/root/scripts/xkeen_rotate.sh --test-notify
```

## Обновление

Для обновления до новой версии просто запустите установку снова:
```bash
curl -sSL https://raw.githubusercontent.com/andrchq/xkeen_auto/main/install.sh | sh
```

Установщик обновит скрипты, сохранив ваши настройки.

## Удаление

```bash
# Удалить задания из cron
crontab -e
# Удалите строки с xkeen_rotate.sh

# Удалить из автозапуска
vi /etc/rc.local
# Удалите строку с xkeen_rotate.sh

# Удалить скрипты
rm -rf /opt/root/scripts/xkeen_*.sh

# Удалить конфигурации (опционально)
rm -rf /opt/etc/xray/outbounds_available
rm -rf /opt/etc/xray/backups
```

## Помощь

Полная документация: [README.md](README.md)

Проблемы и вопросы: [GitHub Issues](https://github.com/andrchq/xkeen_auto/issues)

